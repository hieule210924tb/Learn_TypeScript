const info: {
  name: string;
  age: number;
  address?: string;
  gender: boolean;
} = {
  name: "Hieu",
  age: 21,
  gender: true,
};
console.log(info);
//Dấu ? nằm sau property nghĩa là optional (không bắt buộc)
